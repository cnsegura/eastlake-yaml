apiVersion: v1
kind: Service
metadata:
  name: eastlake
  labels:
    app: eastlake
spec:
  type: LoadBalancer
  selector:
    app: eastlake
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
